/c/ETL_pipeline_in_python
git clone https://github.com/sajidprofessional/python-etl-pipeline.git
 pwd
/c/ETL_pipeline_in_python
/C/ETL_pipeline_in_python/python-etl-pipeline
pwd
mkdir data etl logs
 cd data
 mkdir raw processed
python etl/main.py
bash: $'\E[200~python': command not found

python --version
Python was not found; run without arguments to install from the Microsoft Store, or disable this shortcut from Settings > Apps > Advanced app settings > App execution aliases.

$ py --version
Python 3.13.1
 /C/ETL_pipeline_in_python/python-etl-pipeline (main)-- this will create package
touch etl/__init__.py
$ py etl/main.py
Traceback (most recent call last):
  File "C:\ETL_pipeline_in_python\python-etl-pipeline\etl\main.py", line 4, in <module>
    from etl.extract import extract_data
ModuleNotFoundError: No module named 'etl'
 py -m etl.main
 ls data/processed
clean_output.csv
ls data/raw
patient_data.csv
 cat logs/etl.log
---------------version control 
git add .
git commit -m "Add logging and finalize ETL pipeline"
git push origin main



-------------git add . → stage all changes

------------git commit -m "..." → save snapshot with a message

------------git push origin main → send it to GitHub


---------------- to Rebase change that are not local exists

git pull --rebase origin main

git push origin main

Update your local repo to use the new GitHub URL so you don’t see this message again.

git remote set-url origin https://github.com/sajidprofessional/Python-ETL-Pipeline.git

Verify:

git remote -v

---clean-up (very professional move)
----------tracked files that should NOT be in Git:

----------------__pycache__/

---------------*.pyc

----------logs/etl.log

------------data/processed/clean_output.csv

touch .gitignore

-------------there edit gitignore file for codes

------remove previous file
git rm -r --cached etl/__pycache__
git rm -r --cached data/processed
git rm -r --cached logs/etl.log

git status

git add .gitignore
git commit -m "Clean repo: add .gitignore and remove generated files"


git push origin main

git add -A → stages all changes (new, modified, deleted files)

git commit -m "..." → commits them

git push origin main → pushes to GitHub





